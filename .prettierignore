dist
node_modules
package-lock.json
pnpm-lock.yaml
yarn.lock

# Conductor workspace scratch space, not project source
.context

# Subagent-driven-development scratch space, not project source
.superpowers
